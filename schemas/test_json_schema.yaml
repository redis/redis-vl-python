version: '0.1.0'

index:
    name: json-test
    prefix: json
    storage_type: json

fields:
  - name: sentence
    type: text
  - name: embedding
    type: vector
    attrs:
      dims: 768
      algorithm: flat
      distance_metric: cosine