index:
    name: hash-test
    prefix: hash
    key_separator: ':'
    storage_type: hash

fields:
    text:
        - name: sentence
    vector:
        - name: embedding
          dims: 768
          algorithm: flat
          distance_metric: cosine