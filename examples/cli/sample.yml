

index:
  name: sample
  storage_type: hash
  prefix: "vector:"
  key_field: "id"

fields:
  tag:
    categories:
      separator: "|"
    year:
      separator: "|"
  vector:
    vector:
      initial_cap: 20000
      datatype: "float32"
      algorithm: "flat"
      dims: 768
      distance_metric: "cosine"
